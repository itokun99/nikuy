const auth = {
  LOGOUT: 'LOGOUT'
};

const account = {
  SET_PROFILE: 'SET_PROFILE'
};

const loading = {
  SET_SCREEN_LOADING: 'SET_SCREEN_LOADING'
};

const actionTypes = {
  auth,
  account,
  loading
};

export default actionTypes;